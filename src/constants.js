const path = require('path');

const dataAppConstants = {
  BOOKS_JSON: path.resolve('./data/books.json'),
  AUTHORS_JSON: path.resolve('./data/authors.json'),
  USERS_JSON: path.resolve('./data/users.json'),
};

module.exports = dataAppConstants;
